package A_OUEI.douyin;

// coding1: 一个数组a，可以给其中任意个数加上1然后对a重排列，问能否等于另一个数组b
public class ChongPaiArr {
    boolean rearrange(String a, String b) {
//        if (a.length != b.length)
//            return false;
//
        return true;
    }
}
